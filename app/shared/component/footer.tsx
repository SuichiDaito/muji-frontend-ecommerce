import { defaultConfig } from "next/dist/server/config-shared";

const Footer = () => {
    return (
        <div>
            <p> this is footer for page</p>
        </div>
    );
}

export default Footer;