import { defaultConfig } from "next/dist/server/config-shared";

const Footer = () => {
  return (
    <div className=" px-2 py-4 border border-b mt-2">
      <div className="container mx-auto max-w-7xl">
        <p> this is footer for page</p>
      </div>
    </div>
  );
};

export default Footer;
