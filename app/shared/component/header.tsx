import { defaultConfig } from "next/dist/server/config-shared";

const Header = () => {
    return (
        <div>
            <p> this is header for page</p>
        </div>
    );
}

export default Header;